{
  "name": "@grafana/grizzly-setup",
  "version": "0.0.1",
  "description": "Setup Grizzly - A utility for managing Grafana resources",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --license licenses.txt"
  },
  "keywords": [
    "github",
    "actions",
    "grizzly",
    "grafana"
  ],
  "author": "Grafana Labs",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
